% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_wapor_time_slices.R
\name{get_available_temporal_codes}
\alias{get_available_temporal_codes}
\title{Convert Plan Rows to WaPOR API Query Parameters}
\usage{
get_available_temporal_codes(variable)
}
\arguments{
\item{variable}{Character. Variable name (e.g., \code{"L2-AETI-D"}, \code{"L1-NPP-M"}).}

\item{plan}{A data.frame as returned by \code{\link[=plan_wapor_time_slices]{plan_wapor_time_slices()}}.}
}
\value{
A list of lists, each with \code{code} and a time identifier
(\code{year}, \code{month}, \code{dekad}, or \code{date} as appropriate).

Character vector of available temporal codes (e.g., \code{c("A", "M", "D")}),
ordered from coarsest to finest.
}
\description{
Converts rows from a \code{\link[=plan_wapor_time_slices]{plan_wapor_time_slices()}} output to parameter lists
suitable for WaPOR API queries.
}
\examples{
plan <- plan_wapor_time_slices("2022-10-13", "2023-04-17", avail = c("M", "D"))
params <- plan_to_api_params(plan)
str(params[[1]])
Get Available Temporal Codes for a Variable

Determines which temporal resolutions (annual, monthly, dekadal, daily)
are available for a given WaPOR or AgERA5 variable by checking the
static metadata lists. For L3 variables, checks L2 equivalents as fallback.

get_available_temporal_codes("L1-AETI-D")
# [1] "A" "M" "D"

get_available_temporal_codes("L2-NPP-D")
# [1] "M" "D"
}
