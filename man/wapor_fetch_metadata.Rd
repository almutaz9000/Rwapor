% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wapor_metadata_cache.R
\name{wapor_fetch_metadata}
\alias{wapor_fetch_metadata}
\title{Fetch WaPOR Metadata from JSON Cache Files}
\usage{
wapor_fetch_metadata(level)
}
\arguments{
\item{level}{Character. One of \code{"L1"}, \code{"L2"}, \code{"L3"}, or \code{"all"}.
When \code{"all"} is supplied, all three levels are read and row-bound.}
}
\value{
A data.frame with columns:
\describe{
\item{code}{Character. Dataset code (e.g., \code{"L1-AETI-D"}).}
\item{long_name}{Character. Descriptive name.}
\item{units}{Character. Physical units.}
\item{scale}{Numeric. Scale factor.}
\item{temporal_resolution}{Character. Temporal resolution code.}
\item{spatial_extent}{List column. Named list with \code{xmin}, \code{xmax},
\code{ymin}, \code{ymax}, or \code{NULL} when not available.}
\item{level}{Character. Level identifier (\code{"L1"}, \code{"L2"}, or \code{"L3"}).}
}
}
\description{
Reads pre-built metadata JSON files bundled with the package and returns
a data.frame describing WaPOR Level 1, 2, or 3 datasets.
}
\details{
JSON files are resolved at runtime from a writable user cache first and
fall back to the installed package metadata bundled under \verb{inst/metadata/}.
Call \code{\link[=wapor_update_metadata]{wapor_update_metadata()}} to refresh the user cache from the live API.
}
\examples{
\dontrun{
# Fetch all L1 datasets
meta <- wapor_fetch_metadata("L1")
head(meta)

# Fetch everything at once
all_meta <- wapor_fetch_metadata("all")
}
}
