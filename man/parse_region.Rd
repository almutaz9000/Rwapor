% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{parse_region}
\alias{parse_region}
\title{Parse Region Argument}
\usage{
parse_region(region)
}
\arguments{
\item{region}{One of:
\itemize{
\item Character path to a vector file (shapefile, GeoJSON, GeoPackage, etc.)
\item Character L3 region code (3 uppercase letters, e.g., "AWA")
\item Numeric vector of length 4 representing bounding box: \code{c(xmin, ymin, xmax, ymax)} in WGS84 (EPSG:4326)
}}
}
\value{
A list with components:
\itemize{
\item \code{type}: One of "l3_code", "vector", or "bbox"
\item \code{value}: The parsed region object (character code, sf object, or st_bbox)
}
}
\description{
Parses various region input formats into a standardized structure
for use in other package functions.
}
\examples{
# Parse a bounding box (xmin, ymin, xmax, ymax)
region_info <- parse_region(c(35.0, 33.0, 36.0, 34.0))
region_info$type
# [1] "bbox"

# Parse an L3 region code
region_info <- parse_region("AWA")
region_info$type
# [1] "l3_code"

\dontrun{
# Parse a shapefile
region_info <- parse_region("path/to/region.shp")
region_info$type
# [1] "vector"
}
}
