% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wapor_metadata_cache.R
\name{wapor_update_metadata}
\alias{wapor_update_metadata}
\title{Update WaPOR Metadata Cache from the Live API}
\usage{
wapor_update_metadata(level = "all", dest = NULL)
}
\arguments{
\item{level}{Character. One of \code{"L1"}, \code{"L2"}, \code{"L3"}, or \code{"all"}.
Defaults to \code{"all"}.}

\item{dest}{Character. Directory to write JSON files to. Defaults to the
package's own \verb{inst/metadata/} folder (useful during development). In
production, users can point this to any writable directory.}
}
\value{
Invisibly, a named character vector of file paths that were written
(or would have been written). On network error the function warns and
returns \code{NULL} invisibly without overwriting existing files.
}
\description{
Re-fetches dataset metadata from the FAO GISMGR API and writes JSON cache
files to \code{dest}. This is intended to be run periodically to keep the local
cache up to date when FAO adds new datasets.
}
\details{
\strong{API endpoints used:}
\itemize{
\item L1/L2: \verb{https://data.apps.fao.org/gismgr/api/v2/catalog/workspaces/WAPOR-3/mapsets}
\item L3:    \verb{https://data.apps.fao.org/gismgr/api/v2/catalog/workspaces/WAPOR-3/mosaicsets}
}
Pagination is handled automatically via the \code{links[rel="next"]} field in
each response.
}
\examples{
\dontrun{
# Update all levels in the default package location
wapor_update_metadata()

# Update only L1 and write to a custom directory
wapor_update_metadata("L1", dest = tempdir())
}
}
\keyword{internal}
