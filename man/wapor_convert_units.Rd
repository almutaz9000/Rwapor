% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit_convertor.R
\name{wapor_convert_units}
\alias{wapor_convert_units}
\title{Convert DataFrame Values Between Temporal Units}
\usage{
wapor_convert_units(df, unit_conversion)
}
\arguments{
\item{df}{A data.frame containing columns to convert. Must have:
\itemize{
\item At least one of: \code{mean}, \code{min}, \code{max}, \code{median}
\item \code{start_date}: Date string for calculating conversion factors
\item \code{number_of_days}: Number of days in the source period
}

Must also have a \code{units} attribute specifying the source unit
(e.g., "mm/day", "mm/dekad").}

\item{unit_conversion}{Character. Target temporal unit. One of:
\itemize{
\item "none": No conversion (returns input unchanged)
\item "day": Convert to daily rate
\item "dekad": Convert to 10-day total
\item "month": Convert to monthly total
\item "year": Convert to annual total
}}
}
\value{
The input data.frame with converted values. The following
attributes are updated:
\itemize{
\item \code{units}: New unit string (e.g., "mm/month")
\item \code{original_units}: Original unit string before conversion
}
}
\description{
Converts statistical values (mean, min, max, median) in a data frame
from one temporal unit to another. Commonly used to convert dekadal
values to monthly or annual totals.
}
\details{
Conversion factors:
\itemize{
\item day -> dekad: multiply by number_of_days
\item day -> month: multiply by days in month
\item day -> year: multiply by 365
\item dekad -> month: multiply by 3
\item month -> year: multiply by 12
}
}
\examples{
# Create sample data
df <- data.frame(
  mean = c(2.5, 3.0, 2.8),
  min = c(1.0, 1.5, 1.2),
  max = c(4.0, 4.5, 4.2),
  start_date = c("2023-01-01", "2023-01-11", "2023-01-21"),
  number_of_days = c(10, 10, 11)
)
attr(df, "units") <- "mm/day"

# Convert to monthly totals
df_monthly <- wapor_convert_units(df, "month")
attr(df_monthly, "units")
# [1] "mm/month"
}
