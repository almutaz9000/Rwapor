% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{rwapor_build_crop_assignment_table}
\alias{rwapor_build_crop_assignment_table}
\title{Build Crop Assignment Table}
\usage{
rwapor_build_crop_assignment_table(class_values, crop_defaults = NULL)
}
\arguments{
\item{class_values}{Integer vector of unique crop class values.}

\item{crop_defaults}{Optional data.frame of defaults (same format as
\code{\link{FAO_CROP_DEFAULTS}}). If provided with one row, that row's values are
applied to all classes. If provided with multiple rows, they are matched
positionally (defaults row 1 to first class, defaults row 2 to second class,
etc.). Extra defaults rows beyond the number of classes are silently ignored.}
}
\value{
A data.frame with one row per class, columns for all crop parameters:
\code{class_value}, \code{crop_label}, \code{Kc_ini}, \code{Kc_mid}, \code{Kc_end},
\code{L_ini_days}, \code{L_mid_days}, \code{L_late_days}, \code{max_height_m},
\code{MC}, \code{fc}, \code{AOT}, \code{HI}.
Columns are \code{NA} for classes without a matching defaults row.
}
\description{
Creates a data.frame for each crop class extracted from the mask,
pre-populated with NA parameters. If \code{crop_defaults} is provided,
parameters are filled from it by matching rows positionally (first
defaults row to first class, etc.) or by a single row applied to all
classes.
}
\seealso{
\code{\link{rwapor_extract_crop_classes}}, \code{\link{FAO_CROP_DEFAULTS}},
\code{\link{rwapor_validate_crop_defaults}}
}
