% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_client.R
\name{wapor_generate_urls}
\alias{wapor_generate_urls}
\title{Generate URLs for WaPOR/AgERA5 Resources}
\usage{
wapor_generate_urls(variable, l3_region = NULL, period = NULL)
}
\arguments{
\item{variable}{Character. Variable name following WaPOR/AgERA5 naming
convention (e.g., "L1-AETI-D", "L2-NPP-M", "AGERA5-ET0-E").
The format is \code{{Level}-{Variable}-{TemporalResolution}} where:
\itemize{
\item Level: L1, L2, L3 (WaPOR) or AGERA5
\item Variable: AETI, E, I, NPP, PCP, T, GBWP, NBWP, ET0, TMIN, TMAX, etc.
\item Temporal: D (dekadal), M (monthly), A (annual), E (daily)
}}

\item{l3_region}{Character. Optional L3 region code for Level 3 data
(e.g., "AWA" for Awash Basin). Only applicable for L3 variables.}

\item{period}{Character vector of length 2. Optional date range as
\code{c(start_date, end_date)} in "YYYY-MM-DD" format.}
}
\value{
Character vector of download URLs, sorted chronologically.
}
\description{
Generates download URLs for WaPOR or AgERA5 raster data from the
FAO GISMGR API based on variable name, region, and time period.
}
\examples{
\dontrun{
# Get URLs for dekadal evapotranspiration in January 2023
urls <- wapor_generate_urls(
  variable = "L1-AETI-D",
  period = c("2023-01-01", "2023-01-31")
)

# Get URLs for L3 data in Awash Basin
urls <- wapor_generate_urls(
  variable = "L3-AETI-D",
  l3_region = "AWA",
  period = c("2023-01-01", "2023-03-31")
)

# Get AgERA5 reference evapotranspiration
urls <- wapor_generate_urls(
  variable = "AGERA5-ET0-E",
  period = c("2023-06-01", "2023-06-30")
)
}
}
