% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit_convertor.R
\name{raster_unit_convertor}
\alias{raster_unit_convertor}
\title{Convert Raster Values Between Temporal Units}
\usage{
raster_unit_convertor(r, variable, urls, unit_conversion)
}
\arguments{
\item{r}{A \code{SpatRaster} object from the terra package.}

\item{variable}{Character. Variable name to determine temporal resolution
(e.g., "L1-AETI-D" where "D" indicates dekadal data).}

\item{urls}{Character vector. URLs corresponding to each raster layer,
used to extract date information for conversion factors.}

\item{unit_conversion}{Character. Target temporal unit. One of:
\itemize{
\item "none": No conversion (returns input unchanged)
\item "day": Convert to daily rate
\item "dekad": Convert to 10-day total
\item "month": Convert to monthly total
\item "year": Convert to annual total
}}
}
\value{
The input \code{SpatRaster} with converted values.
}
\description{
Converts raster layer values from one temporal unit to another.
Each layer is converted individually based on its date information.
}
\details{
The function determines the source temporal unit from the variable name:
\itemize{
\item "D" suffix -> dekadal source
\item "M" suffix -> monthly source
\item "A" suffix -> annual source
\item "E" suffix -> daily source
}
}
\examples{
\dontrun{
# Load raster and convert dekadal to monthly
r <- terra::rast(urls)
r_monthly <- raster_unit_convertor(
  r,
  variable = "L1-AETI-D",
  urls = urls,
  unit_conversion = "month"
)
}
}
