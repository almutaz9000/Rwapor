% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_polygon_grid.R
\name{wapor_plot_polygon_grid}
\alias{wapor_plot_polygon_grid}
\title{Plot a raster as a grid of small maps, one per polygon unit}
\usage{
wapor_plot_polygon_grid(
  x,
  polygons,
  id_col,
  label_col = NULL,
  per_page = 24,
  ncol = 6,
  title = NULL,
  legend_title = NULL,
  common_scale = TRUE,
  scale = c("continuous", "percentile", "breaks"),
  limits = NULL,
  probs = c(0.02, 0.1, 0.25, 0.5, 0.75, 0.9, 0.98),
  breaks = NULL,
  palette = "YlGnBu",
  reverse = TRUE,
  line_width = 0.4,
  line_colour = "black",
  buffer = 40,
  square = TRUE,
  mask_outside = FALSE,
  legend_size = 3,
  out_dir = NULL,
  prefix = "polygon_grid",
  width = 12,
  height = 9.5,
  dpi = 300
)
}
\arguments{
\item{x}{SpatRaster or raster file path. Only the first layer is drawn.}

\item{polygons}{sf object, SpatVector or vector file path. Reprojected to the
CRS of \code{x}.}

\item{id_col}{Name of the column that groups polygons into units; one panel
per unique value. Polygons with a missing id are dropped with a warning.}

\item{label_col}{Optional column whose first value per unit is added to the
panel title (for example the crop type).}

\item{per_page, ncol}{Panels per page and per row.}

\item{title}{Page title. Defaults to the layer name of \code{x}. With several
pages, "(page i of n)" is appended.}

\item{legend_title}{Colour bar title. Defaults to \code{title}.}

\item{common_scale}{\code{TRUE} (default): one colour scale shared by all panels.
\code{FALSE}: every panel has its own scale (needs the patchwork package).}

\item{scale}{\code{"continuous"} gradient, \code{"percentile"} classes at the \code{probs}
quantiles of \code{x}, or \code{"breaks"} classes at your \code{breaks}.}

\item{limits}{Range of the continuous scale: \code{NULL} (2nd to 98th
percentile, values outside are drawn in the end colours), \code{"full"}
(minimum to maximum) or two numbers.}

\item{probs}{Quantiles used as class limits when \code{scale = "percentile"}.}

\item{breaks}{Class limits when \code{scale = "breaks"} (at least two).}

\item{palette}{A \code{\link[grDevices:palettes]{grDevices::hcl.pals()}} palette name or a vector of colours.}

\item{reverse}{Reverse the palette direction.}

\item{line_width, line_colour}{Polygon outline width and colour.}

\item{buffer}{Margin around each unit, in metres. Converted to degrees
(1 degree = 111,320 m) when \code{x} has a longitude/latitude CRS.}

\item{square}{\code{TRUE} (default): square panels centred on the unit.
\code{FALSE}: each panel keeps the shape of its unit (needs patchwork).}

\item{mask_outside}{Hide pixels outside the unit's polygons.}

\item{legend_size}{Colour bar size relative to the ggplot2 default.}

\item{out_dir}{Optional folder. Every page is saved as
\verb{<prefix>_page<i>.png}.}

\item{prefix}{File name prefix for saved pages.}

\item{width, height, dpi}{Size (inches) and resolution of saved pages.}
}
\value{
A named list of ggplot (or patchwork) objects, one per page
(\code{page_1}, \code{page_2}, ...), invisibly when \code{out_dir} is supplied.
}
\description{
Groups \code{polygons} into units by \code{id_col} (for example parcels grouped by
farm) and draws one panel per unit: the raster cropped to the unit's extent
plus \code{buffer}, with every polygon outline of the unit drawn on top. Panels
are laid out \code{per_page} to a page, and each page can be saved as a PNG.
}
\details{
The shared colour scale (and the percentile classes) are computed from all
pixels of \code{x}, not only the pixels inside the panels, so the panels use the
same colours as a map of the whole raster. Crop \code{x} first to limit the scale
to an area of interest.
}
\examples{
\dontrun{
pages <- wapor_plot_polygon_grid(
  "seasonal_aeti.tif", "parcels.gpkg", id_col = "farm",
  label_col = "crop", title = "Seasonal AETI", legend_title = "AETI (mm)",
  out_dir = "figures", prefix = "aeti_farms"
)
pages$page_1

# Adequacy in fixed classes, own colours
wapor_plot_polygon_grid(
  adequacy, parcels, id_col = "farm", scale = "breaks",
  breaks = c(0.68, 0.8, 1, 1.2),
  palette = c("#d73027", "#fc8d59", "#fee08b", "#91bfdb", "#4575b4"),
  reverse = FALSE
)
}
}
