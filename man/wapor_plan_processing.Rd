% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing_plan.R
\name{wapor_plan_processing}
\alias{wapor_plan_processing}
\title{Plan How a Raster Job Should Be Processed}
\usage{
wapor_plan_processing(
  template = NULL,
  aoi = NULL,
  resolution = NULL,
  n_layers = 36L,
  n_vars = 1L,
  native_resolution = NULL,
  n_targets = NULL,
  n_profiles = 1L,
  workers = NULL,
  processing = c("auto", "memory", "stream", "tiled")
)
}
\arguments{
\item{template}{Optional \code{SpatRaster} defining the analysis grid.}

\item{aoi}{Optional bounding box \code{c(xmin, ymin, xmax, ymax)} in degrees, used
with \code{resolution} when no template is available.}

\item{resolution}{Analysis resolution in metres (for example 20, 100 or 300),
used with \code{aoi}.}

\item{n_layers}{Integer. Dekads per variable (36 for one year).}

\item{n_vars}{Integer. Number of variables read.}

\item{native_resolution}{Optional numeric vector of each variable's native
resolution in metres. Defaults to the analysis resolution.}

\item{n_targets}{Integer. Outputs accumulated per variable (the season plus
each month). Default \code{1 + ceiling(n_layers / 3)}.}

\item{n_profiles}{Integer. Unique season profiles (crop class, start, end).}

\item{workers}{Integer. Parallel workers. Defaults to \code{\link[future:nbrOfWorkers]{future::nbrOfWorkers()}}.}

\item{processing}{One of \code{"auto"}, \code{"memory"}, \code{"stream"}, \code{"tiled"}.}
}
\value{
An object of class \code{wapor_plan} with the chosen \code{mode}, the
estimate (\code{working_set_bytes}, \code{budget_bytes}), \code{batch_size}, \code{tile_size},
\code{gdal_chunk_bytes}, and the \code{reasons} for the choice.
}
\description{
Estimates the memory a job needs and chooses how to run it:
\itemize{
\item \code{"memory"}: the whole area and every dekad are processed at once.
\item \code{"stream"}: the whole area is processed, reading dekads in batches.
\item \code{"tiled"}: the area is split into square tiles, each streamed separately
and assembled with a VRT. Tiles run in parallel under the active
\code{\link[future:plan]{future::plan()}}.
}
}
\details{
Results are identical whichever mode runs; only memory use and speed differ.

The memory budget is half of the free RAM divided by the number of workers.
Override it with \code{options(Rwapor.memory_budget_mb = ...)}. Mode thresholds
(fractions of the budget) can be changed with
\code{options(Rwapor.plan_thresholds = c(memory = 0.25, stream = 1, layer = 0.1))}.
}
\examples{
# A 5 km x 5 km farm at 20 m for one year: runs in memory
wapor_plan_processing(aoi = c(35, 33, 35.05, 33.05), resolution = 20, n_layers = 36)

# A 150 km x 150 km scheme at 20 m: tiled
wapor_plan_processing(aoi = c(35, 33, 36.5, 34.5), resolution = 20,
                      n_layers = 36, n_vars = 4)
}
