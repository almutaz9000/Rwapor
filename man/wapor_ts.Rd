% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wapor_ts.R
\name{wapor_ts}
\alias{wapor_ts}
\title{Extract Time Series with Zonal Statistics}
\usage{
wapor_ts(
  region,
  variable,
  period,
  identifier = NULL,
  unit_conversion = NULL,
  seasonal = FALSE,
  download_locally = FALSE,
  parallel = FALSE
)
}
\arguments{
\item{region}{Region definition. One of:
\itemize{
\item Path to a vector file (shapefile, GeoJSON, GeoPackage) containing polygons
\item L3 region code (3 uppercase letters, e.g., "AWA")
\item Numeric bounding box: \code{c(xmin, ymin, xmax, ymax)} in WGS84
}}

\item{variable}{Character. Variable name following WaPOR/AgERA5 naming
convention (e.g., "L1-AETI-D", "L2-NPP-M", "AGERA5-ET0-E").}

\item{period}{Character vector of length 2. Date range as
\code{c(start_date, end_date)} in "YYYY-MM-DD" format.}

\item{identifier}{Character. Optional column name in vector file to identify
polygons in output. If NULL, numeric IDs are used.}

\item{unit_conversion}{Character. Target temporal unit for conversion.
One of: "none", "day", "dekad", "month", "year".
Default is NULL, which dynamically sets the default based on variable type:
\itemize{
\item "dekad" for Dekadal variables (files end in "D" but contain daily rates)
\item "none" for others
}}

\item{seasonal}{Logical. If \code{TRUE}, calculates a single seasonal aggregate
(sum/mean) for each polygon over the entire period. Default is \code{FALSE}.}

\item{download_locally}{Logical. Deprecated and ignored. Data are streamed
with \verb{/vsicurl/}. Kept for backward compatibility.}

\item{parallel}{Logical. If \code{TRUE}, attempts to use \code{future.apply} for parallel processing.
Default is \code{FALSE}.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{mean}, \code{min}, \code{max}: Zonal statistics for each polygon/time step
\item \code{start_date}, \code{end_date}: Date range for each time step
\item \code{number_of_days}: Number of days in the time step
\item \code{ID} or custom identifier: Polygon identifier
\item \code{layer_index}: Index of the raster layer
}

The data.frame also has attributes:
\itemize{
\item \code{units}: The unit of measurement (possibly converted)
\item \code{long_name}: Full variable name
\item \code{original_units}: Original units before conversion (if converted)
}
}
\description{
Downloads WaPOR or AgERA5 raster data and extracts time series of zonal
statistics (mean, min, max) for specified polygons or regions.
}
\details{
The function uses \code{exactextractr::exact_extract()} for accurate zonal
statistics that properly handle partial pixel coverage at polygon boundaries.
}
\examples{
\dontrun{
# Extract time series for a bounding box
# For dekadal variables, defaults to mm/dekad (unit_conversion="dekad")
df <- wapor_ts(
  region = c(35.0, 33.0, 36.0, 34.0),
  variable = "L1-AETI-D",
  period = c("2023-01-01", "2023-03-31")
)

# Extract with explicit daily units
df <- wapor_ts(
  region = "fields.geojson",
  variable = "L1-AETI-D",
  period = c("2023-01-01", "2023-12-31"),
  identifier = "field_name",
  unit_conversion = "day"
)

# Check units
attr(df, "units")
attr(df, "long_name")
}
}
