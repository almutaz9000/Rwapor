% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wapor_map.R
\name{wapor_map}
\alias{wapor_map}
\title{Download and Save a Raster Map}
\usage{
wapor_map(
  region,
  variable,
  period,
  folder,
  filename = NULL,
  separate_files = FALSE,
  unit_conversion = NULL,
  seasonal = FALSE,
  mask = FALSE,
  parallel = FALSE,
  batching = TRUE,
  batch_size = NULL,
  l3_region = NULL,
  l3_mode = c("select", "mosaic_all"),
  partial = FALSE,
  cog = FALSE,
  on_batch_done = NULL,
  fun = NULL,
  processing = c("auto", "memory", "stream", "tiled")
)
}
\arguments{
\item{region}{Region definition. One of:
\itemize{
\item Path to a vector file (shapefile, GeoJSON, GeoPackage)
\item L3 region code (3 uppercase letters, e.g., "AWA")
\item Numeric bounding box: \code{c(xmin, ymin, xmax, ymax)} in WGS84
}}

\item{variable}{Character. Variable name following WaPOR/AgERA5 naming
convention (e.g., "L1-AETI-D", "L2-NPP-M", "AGERA5-ET0-E").}

\item{period}{Character vector or list. Date range as
\code{c(start_date, end_date)} in "YYYY-MM-DD" format. Can also be a
named or unnamed list of such vectors for multiple seasons.}

\item{folder}{Character. Output directory path. Will be created if needed.}

\item{filename}{Character. Optional output filename. If NULL, a default
name is generated based on region and variable.}

\item{separate_files}{Logical. If \code{TRUE}, writes each time step as a separate
GeoTIFF file instead of a multi-band stack. In seasonal mode, this saves
seasonal-plan component rasters into
\verb{<folder>/<variable>_seasonal/components/}. Default is \code{FALSE}.}

\item{unit_conversion}{Character. Public unit-conversion mode.
One of: \code{"unit_conversion"} or \code{"none"}.
Default is \code{NULL}, which dynamically matches the variable behavior:
\itemize{
\item Dekadal daily-rate products are saved as dekadal totals
\item Monthly products remain monthly totals
\item \code{"none"} preserves raw API values without temporal conversion
}}

\item{seasonal}{Logical. If \code{TRUE}, downloads and aggregates data for the
entire period into a single seasonal raster (sum/mean).
Default is \code{FALSE}.}

\item{mask}{Logical. If \code{TRUE} and \code{region} is a vector file or polygon,
the output raster is masked to the polygon boundary (pixels outside set to NA).
If \code{FALSE} (default), only a rectangular crop to the bounding box is applied.
Ignored for bounding box and L3 code regions.}

\item{parallel}{Logical. If \code{TRUE}, attempts to use \code{future.apply} for parallel processing.
Default is \code{FALSE}.}

\item{batching}{Logical. If \code{TRUE} (default), processes data in chunks of \code{batch_size}.
If \code{FALSE}, loads all layers at once.}

\item{batch_size}{Integer. Number of remote files loaded per chunk.
Default \code{NULL} lets \code{\link[=wapor_plan_processing]{wapor_plan_processing()}} choose it from the area size
and available memory (all layers at once for small areas). Set a number to
override.}

\item{l3_region}{Optional L3 code to use for an L3 variable and spatial AOI.}

\item{l3_mode}{L3 coverage policy: \code{"select"} requires one selected L3 code
when several regions intersect; \code{"mosaic_all"} writes source assets and a
coverage-bearing mosaic for every intersecting L3 region.}

\item{partial}{Logical. If \code{TRUE}, incomplete temporal coverage is allowed
and recorded. Default \code{FALSE} fails the request.}

\item{cog}{Logical. Write GeoTIFF outputs with \code{\link[=wapor_write_cog]{wapor_write_cog()}}. Default \code{FALSE}.}

\item{on_batch_done}{Optional function called after each processed batch with
\verb{(batch_index, batch_count)}. Callback errors are ignored.}

\item{fun}{Optional seasonal summary function. \code{NULL} (default) preserves
variable-aware weighted aggregation. Explicit \code{"sum"} remains weighted for
accumulative products and is rejected for state/rate products;
\code{"mean"}, \code{"std"}, \code{"min"}, \code{"max"}, and \code{"median"} use each
overlapping source layer once without scaling partial layers.}

\item{processing}{One of \code{"auto"} (default), \code{"memory"}, \code{"stream"} or
\code{"tiled"}. Controls how many layers are held at once; see
\code{\link[=wapor_plan_processing]{wapor_plan_processing()}}.}
}
\value{
Character path to the output GeoTIFF file, or in seasonal mode with
\code{separate_files = TRUE}, a list with \code{seasonal_aggregate} and
\code{seasonal_components}.
}
\description{
Downloads WaPOR or AgERA5 raster data for a specified region and time period,
optionally crops/masks to the region boundary, and saves as a GeoTIFF file.
}
\details{
The function performs the following steps:
\enumerate{
\item Generates download URLs for the specified variable and period
\item Streams raster data using GDAL virtual file system (/vsicurl/)
\item Crops to bounding box or masks to vector geometry
\item Applies temporal-resolution conversion when requested
\item Writes output as a multi-band GeoTIFF (one band per time step) or separate files
}
}
\examples{
\dontrun{
# Download dekadal ET for a bounding box (defaults to mm/dekad)
output_file <- wapor_map(
  region = c(35.0, 33.0, 36.0, 34.0),
  variable = "L1-AETI-D",
  period = c("2023-01-01", "2023-01-31"),
  folder = "output"
)

# Preserve raw API values without temporal conversion
output_file <- wapor_map(
  region = c(35.0, 33.0, 36.0, 34.0),
  variable = "L1-AETI-D",
  period = c("2023-01-01", "2023-01-31"),
  folder = "output",
  unit_conversion = "none"
)

# Download with parallel batching for long periods
library(future)
plan(multisession)
output_file <- wapor_map(
  region = c(35.0, 33.0, 36.0, 34.0),
  variable = "L1-AETI-D",
  period = c("2020-01-01", "2023-12-31"),
  folder = "output",
  parallel = TRUE,
  batch_size = 12
)
}
}
