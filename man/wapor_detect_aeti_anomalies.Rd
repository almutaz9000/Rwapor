% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_anomaly.R
\name{wapor_detect_aeti_anomalies}
\alias{wapor_detect_aeti_anomalies}
\title{Detect AETI Anomalies Relative to Class Median}
\usage{
wapor_detect_aeti_anomalies(
  aeti_seasonal,
  crop_mask,
  threshold = 0.5,
  min_pixels = 30
)
}
\arguments{
\item{aeti_seasonal}{SpatRaster. Seasonal AETI raster.}

\item{crop_mask}{SpatRaster. Crop mask with integer class values.}

\item{threshold}{Numeric. Fraction of median below which pixels are flagged (default 0.5 = 50\%).}

\item{min_pixels}{Integer. Minimum pixels per class to compute reliable median (default 30).}
}
\value{
A list with:
\describe{
\item{anomaly_map}{SpatRaster. 1 = anomaly, 0 = normal, NA = excluded}
\item{anomaly_stats}{data.frame. Per-class anomaly statistics}
\item{threshold_raster}{SpatRaster. Threshold values per class}
}
}
\description{
Identifies pixels where AETI is significantly below the class median,
indicating potential water stress or irrigation failures.
}
\examples{
\dontrun{
anomalies <- wapor_detect_aeti_anomalies(seasonal_aeti, crop_mask, threshold = 0.6)
plot(anomalies$anomaly_map, main = "Water Stress Anomalies")
}
}
