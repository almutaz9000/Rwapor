% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wapor_res_key.R
\name{wapor_res_key}
\alias{wapor_res_key}
\title{Get the native-grid resolution key for a WaPOR / AgERA5 variable}
\usage{
wapor_res_key(variable)
}
\arguments{
\item{variable}{Character scalar. Variable code such as \code{"L1-AETI-D"},
\code{"L1-PCP-D"}, \code{"L2-AETI-D"}, \code{"L3-AETI-D"}, or \code{"AGERA5-ET0-E"}.}
}
\value{
A character scalar, e.g. \code{"L1_300m"}, \code{"L1_5000m"}, \code{"L2_100m"},
\code{"L3_30m"}, \code{"AGERA5_11000m"}.  Returns \code{variable} itself (unique key) if
not recognised, which safely prevents it from being batched with others.
}
\description{
Returns a short string that identifies the native pixel grid of a variable.
Variables that share the same key are on the same spatial grid and can safely
be stacked for a single zonal-statistics pass.  Variables with \emph{different}
keys must be extracted independently.
}
\details{
Within Level 1, variables originate from different sensors:
\itemize{
\item AETI / E / T / I / NPP / TBP / GBWP / NBWP — MODIS (~300 m)
\item PCP — CHIRPS (~5 000 m)
\item RET — ERA5 (~30 000 m)
\item RSM — Sentinel-1 (~500 m)
}

Grouping L1 variables by level prefix alone would be incorrect because they
cannot be stacked onto the same grid without resampling.
}
\examples{
wapor_res_key("L1-AETI-D")   # "L1_300m"
wapor_res_key("L1-PCP-D")    # "L1_5000m"
wapor_res_key("L1-RET-D")    # "L1_30000m"
wapor_res_key("L2-AETI-D")   # "L2_100m"
wapor_res_key("L3-AETI-D")   # "L3_30m"
wapor_res_key("AGERA5-ET0-E") # "AGERA5_11000m"
}
