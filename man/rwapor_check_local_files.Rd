% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{rwapor_check_local_files}
\alias{rwapor_check_local_files}
\title{Check for Local Raster Files}
\usage{
rwapor_check_local_files(urls, var, folder)
}
\arguments{
\item{urls}{Character vector of WaPOR URLs.}

\item{var}{Character. WaPOR variable code (e.g., \code{"L1-AETI-D"}).}

\item{folder}{Character. Path to the local analysis folder.}
}
\value{
A list with components:
\describe{
\item{optimized_paths}{Character vector of paths to use in \code{terra::rast()}
(local file paths for files found locally, or \code{/vsicurl/} prefixed URLs
for files not yet downloaded).}
\item{missing_dates}{Character vector of dates (\code{"YYYY-MM-DD"}) for which
no local file was found.}
\item{found_count}{Integer. Number of time steps found locally.}
}
}
\description{
Given a set of WaPOR URLs and a local folder, checks which files exist
locally following the standard naming convention used by \code{\link{wapor_map}}.
Returns a vector of optimized paths that substitutes local files where
available and falls back to remote streaming (\code{/vsicurl/}) otherwise.
}
\seealso{
\code{\link{wapor_map}}
}
