% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_polygon_seasons.R
\name{wapor_plot_polygon_seasons}
\alias{wapor_plot_polygon_seasons}
\title{Plot selected polygon units over several raster layers}
\usage{
wapor_plot_polygon_seasons(
  x,
  polygons,
  id_col,
  ids = NULL,
  row_labels = NULL,
  col_labels = NULL,
  title = NULL,
  legend_title = NULL,
  scale = c("continuous", "percentile_stretch"),
  limits = NULL,
  probs = c(0.02, 0.1, 0.25, 0.5, 0.75, 0.9, 0.98),
  palette = "YlGnBu",
  reverse = TRUE,
  line_width = 0.4,
  line_colour = "black",
  buffer = 40,
  legend_size = 3,
  out_file = NULL,
  width = 14,
  height = NULL,
  dpi = 300
)
}
\arguments{
\item{x}{SpatRaster (or raster file path) with one layer per column.}

\item{polygons}{sf object, SpatVector or vector file path. Reprojected to the
CRS of \code{x}.}

\item{id_col}{Name of the column that groups polygons into units.}

\item{ids}{Units to draw, one row each, in this order. Defaults to all units
(sorted).}

\item{row_labels}{Row titles, one per \code{ids}, unique. Defaults to \code{ids}.}

\item{col_labels}{Column titles, one per layer of \code{x}. Defaults to the layer
names.}

\item{title}{Plot title.}

\item{legend_title}{Colour bar title. Defaults to \code{title}.}

\item{scale}{\code{"continuous"} gradient spread evenly between \code{limits}, or
\code{"percentile_stretch"} gradient anchored at the \code{probs} quantiles.}

\item{limits}{Range of the continuous scale: \code{NULL} (2nd to 98th
percentile, values outside are drawn in the end colours), \code{"full"}
(minimum to maximum) or two numbers. Not used for \code{"percentile_stretch"},
whose range is the first to last quantile.}

\item{probs}{Quantiles used as colour anchors when
\code{scale = "percentile_stretch"} (at least two, distinct values).}

\item{palette}{A \code{\link[grDevices:palettes]{grDevices::hcl.pals()}} palette name or a vector of colours.}

\item{reverse}{Reverse the palette direction.}

\item{line_width, line_colour}{Polygon outline width and colour.}

\item{buffer}{Margin around each unit, in metres. Converted to degrees
(1 degree = 111,320 m) when \code{x} has a longitude/latitude CRS.}

\item{legend_size}{Colour bar length relative to the ggplot2 default. With a
single row the colour bar is as tall as the panels.}

\item{out_file}{Optional PNG path to save the plot.}

\item{width, height, dpi}{Size (inches) and resolution of the saved plot.
\code{height = NULL} sizes it from the number of rows and columns.}
}
\value{
A ggplot object, invisibly when \code{out_file} is supplied.
}
\description{
Draws a grid of small maps with one row per polygon unit (for example a
farm) and one column per layer of \code{x} (for example one seasonal AETI map per
season). Every panel of a row shows the same unit at the same size: the
layer cropped to the unit's square extent plus \code{buffer}, with all polygon
outlines of the unit on top. All panels share one colour scale, computed
from all pixels of all layers of \code{x}, so colours can be compared between
units and between layers.
}
\details{
\code{scale = "percentile_stretch"} keeps one continuous gradient but anchors its
colours at the \code{probs} quantiles of \code{x} instead of spreading them evenly
between the limits: the colour changes fastest where most pixels are, so
differences stand out more. The colour bar ticks are the anchor values.
}
\examples{
\dontrun{
seasons <- terra::rast(c("aeti_2018.tif", "aeti_2019.tif", "aeti_2020.tif"))
names(seasons) <- c("2018/2019", "2019/2020", "2020/2021")
wapor_plot_polygon_seasons(
  seasons, "parcels.gpkg", id_col = "farm", ids = c("F053", "F008", "F058"),
  row_labels = c("F053 above ETc", "F008 changes", "F058 below ETc"),
  title = "Seasonal AETI", legend_title = "Seasonal AETI\n(mm)",
  scale = "percentile_stretch", out_file = "sample_farms.png"
)
}
}
\seealso{
\code{\link[=wapor_plot_polygon_grid]{wapor_plot_polygon_grid()}} for one panel per unit and one layer.
}
