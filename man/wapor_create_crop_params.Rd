% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_defaults.R
\name{wapor_create_crop_params}
\alias{wapor_create_crop_params}
\title{Create Custom Crop Parameters for Analysis}
\usage{
wapor_create_crop_params(
  class_value = 1L,
  crop_name = "Custom Crop",
  kc_ini = 0.35,
  kc_mid = 1.15,
  kc_end = 0.35,
  l_ini_days = 25L,
  l_mid_days = 45L,
  l_late_days = 30L,
  max_height_m = 1,
  HI = 0.45,
  MC = 0.12,
  fc = 1,
  AOT = 0.8,
  region = "Custom",
  notes = "User customized"
)
}
\arguments{
\item{class_value}{Integer. Raster class code in the crop mask.}

\item{crop_name}{Character. Descriptive name of the crop.}

\item{kc_ini}{Numeric. Initial stage crop coefficient (0.05 - 1.5).}

\item{kc_mid}{Numeric. Mid-season crop coefficient (0.1 - 2.0).}

\item{kc_end}{Numeric. Late/end stage crop coefficient (0.05 - 1.5).}

\item{l_ini_days}{Integer. Length of initial stage in days (>= 1).}

\item{l_mid_days}{Integer. Length of mid-season stage in days (>= 1).}

\item{l_late_days}{Integer. Length of late stage in days (>= 1).}

\item{max_height_m}{Numeric. Maximum crop height in meters (default: 1.0).}

\item{HI}{Numeric. Harvest Index (0.0 - 1.0, default: 0.45).}

\item{MC}{Numeric. Moisture Content of harvested crop (0.0 - 1.0, default: 0.12).}

\item{fc}{Numeric. Ground cover / light interception factor (default: 1.0).}

\item{AOT}{Numeric. Aboveground to total biomass ratio (default: 0.8).}

\item{region}{Character. Geographic or agronomic region description.}

\item{notes}{Character. User notes or metadata.}
}
\value{
A validated single-row data.frame ready for use in \code{wapor_run_seasonal_analysis()}.
}
\description{
Builds a validated \code{crop_params} data.frame of crop growth and production
parameters for a specific raster class, without relying on FAO default datasets.
}
\examples{
cp <- wapor_create_crop_params(
  class_value = 1L,
  crop_name = "Local Durum Wheat",
  kc_ini = 0.35,
  kc_mid = 1.20,
  kc_end = 0.28,
  l_ini_days = 25L,
  l_mid_days = 45L,
  l_late_days = 30L,
  HI = 0.48
)
}
