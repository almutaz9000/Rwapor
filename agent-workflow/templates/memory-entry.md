- **<short label>**: <the confirmed, durable lesson — a working pattern, a
  recurring pitfall, or a stable constraint>. <optional: file:line reference
  or issues-log ID for more detail>

<!-- Only add an entry here once something is CONFIRMED, not while still
     being investigated. Prefer editing/merging an existing entry over
     appending a near-duplicate. This file is curated, not a log. -->
