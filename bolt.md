# Jules / Bolt Adapter

Jules sessions and Bolt.new integrations in this repo must start with `agent-workflow/START-HERE.md`.

Use this file for session-specific or tool-specific runtime guidance. The canonical project workflow, memory, task state, and issue state live under `agent-workflow/`.

## Online Work Queue

- Use unchecked items in `agent-workflow/task-status.md` as the default pending work queue.
- Read `agent-workflow/session-brief.md` for the latest context.
- Log confirmed root causes, regressions, and resolutions in `agent-workflow/issues-log.md`.
