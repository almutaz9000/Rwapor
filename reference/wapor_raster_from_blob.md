# Extract Raster from DuckDB BLOB

Extract Raster from DuckDB BLOB

## Usage

``` r
wapor_raster_from_blob(blob_data)
```

## Arguments

- blob_data:

  Raw vector containing raster BLOB

## Value

terra SpatRaster object
