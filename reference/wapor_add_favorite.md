# Add a Path to Favorites

Add a Path to Favorites

## Usage

``` r
wapor_add_favorite(path, type = c("directory", "file"))
```

## Arguments

- path:

  Character. Path to a directory or file.

- type:

  Character. One of "directory" or "file".

## Value

Logical. TRUE if successful.
