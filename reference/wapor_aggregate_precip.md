# Aggregate Precipitation to Monthly Totals

Aggregate Precipitation to Monthly Totals

## Usage

``` r
wapor_aggregate_precip(precip_ts)
```

## Arguments

- precip_ts:

  data.frame with columns: date, value (daily or dekadal precip).

## Value

A data.frame with columns: year, month, p_monthly_mm.
