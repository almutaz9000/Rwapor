# Detect season windows from local seasonal raster folders

Detect season windows from local seasonal raster folders

## Usage

``` r
wapor_detect_folder_seasons(folder)
```

## Arguments

- folder:

  Character path to the project folder.

## Value

List with `seasonal_dirs` and `windows`.
