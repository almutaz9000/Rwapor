# Check Raster Spatial Overlap

Tests if two rasters have spatial overlap (after reprojection if
needed).

## Usage

``` r
.check_raster_overlap(r1, r2)
```

## Arguments

- r1:

  SpatRaster. First raster.

- r2:

  SpatRaster. Second raster.

## Value

Logical. TRUE if rasters overlap.
