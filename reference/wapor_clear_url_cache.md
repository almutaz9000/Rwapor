# Clear WaPOR Disk URL Cache

Removes all cached API response files from the user's cache directory.

## Usage

``` r
wapor_clear_url_cache()
```

## Value

Invisible integer count of removed cache files.

## Examples

``` r
wapor_clear_url_cache()
```
