# Parse Shiny batch input into named season periods

Parse Shiny batch input into named season periods

## Usage

``` r
wapor_parse_batch_periods(batch_text)
```

## Arguments

- batch_text:

  Character scalar with one `Label, Start, End` entry per line.

## Value

List with `periods` and `season_table`.
