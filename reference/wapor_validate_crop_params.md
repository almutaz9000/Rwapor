# Validate Crop Parameters

Checks crop parameter data.frame for valid ranges and required columns.

## Usage

``` r
wapor_validate_crop_params(crop_params)
```

## Arguments

- crop_params:

  data.frame. Crop parameters.

## Value

Character vector of error messages (empty if valid).
